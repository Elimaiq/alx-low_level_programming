#include <stdio.h>
/**
 * main - contains the code that prints our output
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
